% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_enrichment.R
\name{add_unit_sizes}
\alias{add_unit_sizes}
\title{Add unit resource sizes to log-derived unit data}
\usage{
add_unit_sizes(
  unit_data,
  sizes,
  unit_col = NULL,
  size_name_col = "name",
  size_col = "total_size"
)
}
\arguments{
\item{unit_data}{Tibble. Unit-level or unit-playback data, for example
returned by \code{\link[=estimate_unit_times]{estimate_unit_times()}} or state-specific log summaries.}

\item{sizes}{Tibble. Output of \code{\link[=compute_sizes]{compute_sizes()}}.}

\item{unit_col}{Optional name of the unit identifier column in \code{unit_data}.
Defaults to \code{unit_key} when available, otherwise \code{unit_alias}.}

\item{size_name_col}{Name column in \code{sizes}. Defaults to \code{name}, matching
\code{\link[=compute_sizes]{compute_sizes()}} output.}

\item{size_col}{Size column in \code{sizes}. Defaults to \code{total_size}, matching
\code{\link[=compute_sizes]{compute_sizes()}} output.}
}
\value{
A tibble.
}
\description{
\ifelse{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental"><img src="figures/lifecycle-experimental.svg" alt="[Experimental]" /></a>}}{\strong{[Experimental]}}

Joins unit resource sizes from \code{\link[=compute_sizes]{compute_sizes()}} to log-derived unit data.
Unit names are matched both with and without a trailing \code{.xml} extension.
The function adds byte and MiB sizes and, when load-time columns are present,
observed load-time-per-MiB indicators. These indicators are descriptive
load-time ratios and should not be interpreted as download speed.
}
