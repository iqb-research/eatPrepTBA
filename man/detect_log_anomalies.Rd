% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_analysis.R
\name{detect_log_anomalies}
\alias{detect_log_anomalies}
\title{Detect potentially unreliable log patterns}
\usage{
detect_log_anomalies(
  logs,
  session_cols = NULL,
  unit_cols = NULL,
  focus_loss_threshold_ms = 5 * 60 * 1000,
  connection_transition_threshold = 10L,
  include_unknown_events = FALSE
)
}
\arguments{
\item{logs}{Tibble. Logs retrieved with \code{\link[=get_logs]{get_logs()}} or read with \code{\link[=read_logs]{read_logs()}}.}

\item{session_cols}{Optional character vector with columns defining a test
session. By default, all available columns among \code{group_id}, \code{group},
\code{login_name}, \code{login}, \code{login_code}, and \code{booklet_id} are used.}

\item{unit_cols}{Optional character vector with columns defining units. By
default, available columns among \code{unit_key} and \code{unit_alias} are used.}

\item{focus_loss_threshold_ms}{Numeric. Focus losses longer than this
threshold are flagged.}

\item{connection_transition_threshold}{Integer. Sessions with more
connection state transitions than this threshold are flagged.}

\item{include_unknown_events}{Logical. Should event types unknown to
eatPrepTBA be returned as informational anomalies? Defaults to \code{FALSE}
because large log datasets may contain many player-specific entries.}
}
\value{
A tibble with session identifiers, optional unit identifiers,
\code{anomaly_code}, \code{severity}, \code{ts_start}, \code{ts_end}, \code{n_events}, \code{evidence}, and
\code{message}.
}
\description{
\ifelse{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental"><img src="figures/lifecycle-experimental.svg" alt="[Experimental]" /></a>}}{\strong{[Experimental]}}

Returns a long evidence table with one row per detected log anomaly. The
function is intentionally conservative: it focuses on structural reliability
signals that can be derived from ordinary Testcenter logs, such as malformed
\code{LOADCOMPLETE} rows, player loading/running inconsistencies, connection loss,
unresolved focus loss, runtime errors, timestamp problems, and inconsistent
page counters.
}
