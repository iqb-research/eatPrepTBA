% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_booklets_from_block_design.R
\name{prepare_booklets_from_block_design}
\alias{prepare_booklets_from_block_design}
\title{Prepare booklet specifications from a tabular block design}
\usage{
prepare_booklets_from_block_design(
  booklets,
  blocks,
  units,
  restrictions = NULL,
  booklet_subject_fn = NULL,
  add_start_end = TRUE,
  booklet_filter = NULL,
  wrap_blocks = TRUE,
  default_leave = NULL,
  keep_leave_without_minutes = FALSE
)
}
\arguments{
\item{booklets}{A tibble with one row per booklet. It must contain either
\code{booklet_id} or \code{booklet} and one or more \verb{block_*} columns containing the
block ids in booklet order. \code{booklet_label}, \code{booklet_description}, and
\code{booklet_configuration} are optional.}

\item{blocks}{A tibble with one row per block. It must contain \code{block} and
may contain \code{subject}, \code{domain}, \code{minutes}, \code{testlet_id}, \code{testlet_label},
and \code{wrap}. Missing \code{subject} or \code{domain} values apply to all matching
unit subjects or domains.}

\item{units}{A tibble with one row per unit. It must contain \code{block} and
\code{unit_key}; \code{subject}, \code{domain}, \code{sequence}, \code{unit_alias}, \code{unit_label},
and \code{unit_labelshort} are optional.}

\item{restrictions}{An optional tibble with testlet restriction information.
It may contain \code{booklet_id}, \code{subject}, \code{domain}, \code{block}, \code{testlet_id},
\code{testlet_label}, \code{minutes}, \code{leave}, \code{code}, \code{code_to_enter},
\code{presentation}, \code{response}, and \code{wrap}. Missing \code{subject} or \code{domain}
values apply to all matching unit subjects or domains. It can also use the
compact timing format \code{design}, \code{block}, \code{seconds}, and optional
\code{block_group} and \code{leave}. \code{design} is matched as an underscore-delimited
part of \code{booklet_id}. \code{block} contains one booklet position column, such as
\code{"block_4"}. If \code{block_group} is present, rows with the same \code{design} and
\code{block_group} are combined into one timed testlet and \code{block_group} is used
as the testlet label. If \code{block_group} is missing or empty, each \code{block}
becomes its own timed testlet. If \code{leave} is missing or empty in this compact
format, it defaults to \code{"allowed"}.}

\item{booklet_subject_fn}{Optional function that receives \code{booklet_id} and
returns a subject used to disambiguate blocks that exist for several
subjects.}

\item{add_start_end}{Logical. Should \code{Start_page} and \code{End_page} units be
added to every booklet?}

\item{booklet_filter}{Optional character vector of booklet ids to keep.}

\item{wrap_blocks}{Logical. Should blocks be wrapped into testlets by
default?}

\item{default_leave}{Optional default value for the \code{leave} attribute of
\code{TimeMax} restrictions.}

\item{keep_leave_without_minutes}{Logical. Should \code{leave} be kept when no
\code{minutes} restriction is present?}
}
\value{
A tibble that can be passed to \code{\link[=generate_booklets]{generate_booklets()}}.
}
\description{
Prepare booklet specifications from a tabular block design
}
