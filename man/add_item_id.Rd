% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_item_id.R
\name{add_item_id}
\alias{add_item_id}
\title{Add item identifiers to variable-level data}
\usage{
add_item_id(data, units, overwrite = FALSE)
}
\arguments{
\item{data}{Data frame containing \code{unit_key} and \code{variable_id}.}

\item{units}{Data frame returned by \code{\link[=get_units]{get_units()}} with \code{metadata = TRUE}.
Uses its \code{items_list} list-column, or \code{item_metadata} after \code{\link[=add_metadata]{add_metadata()}}.}

\item{overwrite}{Logical. Replace an existing \code{item_id} column? Defaults to
\code{FALSE}, which raises an error if the column already exists.}
}
\value{
\code{data} with an \code{item_id} character column. Row count and order are
preserved. With \code{overwrite = TRUE}, unmatched rows replace old IDs with \code{NA}.
}
\description{
Adds the item identifier stored in Studio metadata by matching \code{unit_key}
and \code{variable_id}. No API requests or coding operations are performed.
The player definition alone does not contain this metadata mapping.
}
\details{
Missing or empty keys and item identifiers are ignored in the lookup.
Unmatched rows receive \code{NA_character_}; no item identifiers are invented.
Repeated identical mappings are collapsed. Multiple distinct item identifiers
for the same pair of keys anywhere in \code{units} cause an error, including
conflicts between workspaces. Subset \code{units} to the relevant versions first.
When both metadata columns exist, \code{items_list} is used.
}
\examples{
units <- tibble::tibble(
  unit_key = "U1",
  items_list = list(tibble::tibble(variable_id = "V1", item_id = "I1"))
)
responses <- tibble::tibble(unit_key = c("U1", "U1"),
                            variable_id = c("V1", "V2"))
add_item_id(responses, units)
}
