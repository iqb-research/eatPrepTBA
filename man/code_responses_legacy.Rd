% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_responses_legacy.R
\name{code_responses_legacy}
\alias{code_responses_legacy}
\title{Code unit responses with coding schemes}
\usage{
code_responses_legacy(
  responses,
  units,
  prepare = FALSE,
  by = NULL,
  codes_manual = NULL,
  missings = NULL,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{responses}{Tibble. Response data retrieved from the IQB Testcenter with setting the argument \code{prepare = FALSE} for \code{\link[=get_responses]{get_responses()}} or \code{\link[=read_responses]{read_responses()}}.}

\item{units}{Tibble. Unit data retrieved from the IQB Studio after setting the argument \code{coding_scheme = TRUE} for \code{\link[=get_units]{get_units()}}.}

\item{prepare}{Logical. Whether to unpack the coding results and to add information from the coding schemes.}

\item{by}{Character. Additional columns as subgroups for the coding (e.g., in case of duplicate unit data for a specific person that could emerge in offline settings).}

\item{codes_manual}{Tibble (optional). Data frame holding the manual codes. Defaults to \code{NULL} and does only automatic coding.}

\item{missings}{Tibble (optional). Provide missing meta data with \code{code_id}, \code{status}, \code{code_score}, and \code{code_type}. Defaults to \code{NULL} and uses default scheme.}

\item{parallel}{Logical. Should the coding be conducted on multiple cores?}

\item{n_cores}{Integer. Number of the cores to be used for coding (only relevant if \code{parallel = TRUE}). Will default to number of available system cores - 1.}
}
\value{
A tibble.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}

This function automatically codes responses by using the \code{eatAutoCode} package.
}
\keyword{internal}
