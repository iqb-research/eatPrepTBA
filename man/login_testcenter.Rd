% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/login_testcenter.R
\name{login_testcenter}
\alias{login_testcenter}
\title{Generate a \linkS4class{LoginTestcenter} object for the IQB Testcenter}
\usage{
login_testcenter(
  base_url = "https://iqb-testcenter2.de/",
  keyring = FALSE,
  change_key = FALSE,
  dialog = TRUE,
  insecure = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{base_url}{Character. Base URL of the hosted instance of the IQB Testcenter. Default is the https://iqb-testcenter2.de/.}

\item{keyring}{Logical. Should the \link[keyring:keyring-package]{keyring::keyring} package be used to save the passkey? This saves your credentials to your local machine. Defaults to \code{FALSE}.}

\item{change_key}{Logical. If your password on the domain has changed - should the \link[keyring:keyring-package]{keyring::keyring} password be changed? Defaults to \code{FALSE}.}

\item{dialog}{Logical. Should the password be entered using the RStudio dialog (\code{TRUE}) or using the console (\code{FALSE}). Defaults to \code{TRUE}.}

\item{insecure}{Logical. Should the https security certificate be ignored (only recommended for Intranet requests that might not have a valid security certificate).}

\item{verbose}{Logical. If \code{TRUE}, additional information is printed. Defaults to \code{FALSE}.}
}
\value{
An object of the \linkS4class{LoginTestcenter} class.
}
\description{
Provides a routine to login to an instance of the IQB Testcenter.
}
\details{
Calling the \code{login_testcenter()} function generates the following curl request
on the \code{base_url} (default is https://iqb-testcenter2.de/api) with the \code{name} and
the \code{password} provided by the user:

\if{html}{\out{<div class="sourceCode">}}\preformatted{curl --location --request PUT '\{base_url\}/session/admin'
--header 'Content-Type: application/json'
--data '\{
    "name": "\{name\}",
    "password": "\{password\}"
\}'
}\if{html}{\out{</div>}}

Note that the name and the password are only available to the function call
and cannot be accessed later as they are not part of the \linkS4class{Login} object generated.
}
