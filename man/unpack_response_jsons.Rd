% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unpack_response_jsons.R
\name{unpack_response_jsons}
\alias{unpack_response_jsons}
\title{Unpack response JSON columns}
\usage{
unpack_response_jsons(
  responses,
  response_cols = NULL,
  id_cols = NULL,
  keep_empty = FALSE
)
}
\arguments{
\item{responses}{Data frame. Response data with one or more JSON columns.}

\item{response_cols}{Character vector. Columns to unpack. If \code{NULL}, columns
are auto-detected by parsing non-empty JSON payloads that contain response
record fields such as \code{id}, \code{status}, \code{value}, \code{subform}, \code{code}, and
\code{score}.}

\item{id_cols}{Character vector. Columns to keep as identifiers. If \code{NULL},
all non-JSON and non-timestamp columns are kept.}

\item{keep_empty}{Logical. Whether missing, empty, and \verb{[]} payloads should
be kept as rows with missing response identifiers.}
}
\value{
A tibble with the identifier columns, response slot metadata, and the
parsed JSON fields.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

This function unpacks response JSON payloads that are distributed across
several columns, for example \code{question_0_content}, \code{sums_content}, or
\code{responses}. Matching timestamp columns are detected by replacing a
\verb{_content} suffix with \verb{_ts}, for example \code{question_0_content} is matched to
\code{question_0_ts}. This is particularly useful for BKT-like question-slot
preparation where coded responses are distributed across \code{question_*_content}
columns instead of one \code{coded} column.
}
