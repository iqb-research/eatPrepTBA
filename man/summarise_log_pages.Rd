% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_metrics.R
\name{summarise_log_pages}
\alias{summarise_log_pages}
\title{Summarise page state logs}
\usage{
summarise_log_pages(logs, session_cols = NULL, unit_cols = NULL)
}
\arguments{
\item{logs}{Tibble. Logs retrieved with \code{\link[=get_logs]{get_logs()}} or read with \code{\link[=read_logs]{read_logs()}}.}

\item{session_cols}{Optional character vector with columns defining a test
session. By default, all available session columns are used.}

\item{unit_cols}{Optional character vector with columns defining units. By
default, available columns among \code{unit_key} and \code{unit_alias} are used.}
}
\value{
A tibble with one row per session/unit.
}
\description{
\ifelse{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental"><img src="figures/lifecycle-experimental.svg" alt="[Experimental]" /></a>}}{\strong{[Experimental]}}

Summarises raw \code{CURRENT_PAGE_ID}, \code{CURRENT_PAGE_NR}, and \code{PAGE_COUNT} events
per session/unit. \code{reached_last_page_nr} only checks whether the maximum
observed page number reached the reported page count. \code{observed_pages_complete}
is stricter and is only \code{TRUE} when all integer page numbers from 1 to
\code{PAGE_COUNT} were observed and the page count was consistent; otherwise it is
\code{FALSE} for visible gaps or \code{NA} when completeness cannot be judged. Negative
\code{CURRENT_PAGE_ID} values are counted separately because they usually indicate
a Testcenter state where the player-reported page could not be mapped to the
current valid pages.
}
