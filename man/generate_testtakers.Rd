% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_testtakers.R
\name{generate_testtakers}
\alias{generate_testtakers}
\title{Generate Testcenter testtakers XML}
\usage{
generate_testtakers(
  testtakers,
  custom_texts = NULL,
  profiles = NULL,
  app_version = "18.0.0",
  login = NULL,
  testtakers_version = c("18.0", "legacy-16")
)
}
\arguments{
\item{testtakers}{Data frame in the eatPrepTBA testtakers target format.
It must contain \code{group_id} and \code{login_name}. In current Testcenter 18
output, \code{group_label} is required by the XML specification; missing labels
are filled from \code{group_id} with a warning. Optional columns are
\code{group_label}, \code{group_valid_to}, \code{group_valid_from}, \code{group_valid_for},
\code{login_pw}, \code{login_mode}, \code{login_monitor_code}, \code{booklet_id},
\code{booklet_codes}, \code{booklet_state}, and \code{profile_id}.}

\item{custom_texts}{Optional named list of Testcenter custom text keys and
replacement strings. For example, \code{list(AppTitle = "Pilot")} becomes a
\code{CustomText} node with key \code{AppTitle}.}

\item{profiles}{Optional data frame defining group-monitor profiles. It must
contain \code{profile_id} when supplied. Optional columns are \code{profile_label},
\code{block_column}, \code{unit_column}, \code{view}, \code{group_column}, \code{booklet_column},
\code{booklet_states_columns}, \code{filter_pending}, \code{filter_locked},
\code{autoselect_next_block}, \code{filter_label}, \code{filter_field}, \code{filter_type},
\code{filter_value}, \code{filter_sub_value}, and \code{filter_not}.}

\item{app_version}{Version of the target Testcenter instance. Defaults to
\code{"18.0.0"}. This is used for legacy raw-GitHub schema URLs; current
Testcenter 18 output uses \code{testtakers_version} for the \code{w3id.org} schema
URL.}

\item{login}{Target Testcenter instance. If it is available, the
\code{app_version} will be overwritten.}

\item{testtakers_version}{Testtakers XML specification version. \code{"18.0"}
emits the current Testcenter testtakers schema URL. \code{"legacy-16"} emits
the legacy Testcenter 16 schema URL used by older workflows.}
}
\value{
An \code{xml_document} containing a Testcenter \code{Testtakers} XML document.
}
\description{
Generate Testcenter testtakers XML
}
\details{
This function expects the final target table for a Testcenter testtakers
file. Project-specific wrappers can read Excel files, join booklet designs,
expand days or parts, and create monitor logins, but they should eventually
hand over a \code{testtakers} data frame with one row per login/booklet or
login/profile assignment.

Rows with the same \code{group_id} form one \code{Group}. Rows with the same
\code{login_name} form one \code{Login}; a \code{login_name} must not be reused across
groups. If \code{booklet_id} is present, \code{Booklet} children are added to the
login. If \code{profile_id} is present, \code{Profile} references are added instead.
In Testcenter 18.0 output, one login cannot mix \code{Booklet} and \code{Profile}
children.

\code{generate_testtakers()} returns an XML document only. It does not read input
files or write \code{testtakers.xml}; wrapper functions should call
\code{xml2::write_xml()} if a file should be created.

\code{custom_texts} is passed through to the XML as named \code{CustomText} nodes and
can be an extensive project-specific list. Typical keys override text in the
booklet player (\code{booklet_*}), group monitor (\code{gm_*}), and login screen
(\code{login_*}). Values can contain longer or multi-line messages. Keep
Testcenter placeholders such as \code{\%s} unchanged when replacing texts. The
function checks that the list is named, but it does not validate the key set
against a particular Testcenter version; unsupported or misspelled keys are
handled by Testcenter.
}
\examples{
testtakers <- tibble::tibble(
  group_id = "G1",
  group_label = "Group 1",
  login_name = "student01",
  login_pw = "pw01",
  login_mode = "run-hot-return",
  booklet_id = "BOOKLET_1",
  booklet_codes = "A1 B2"
)

custom_texts <- list(
  booklet_codeToEnterPrompt = "Please enter the release code.",
  booklet_msgSoonTimeOver = "You have \%s minute(s) left for this section.",
  gm_col_personLabel = "Login/student code",
  gm_control_pause = "Pause",
  login_codeInputTitle = "Enter student code"
)

# Wrapper for the current Testcenter 18 testtakers XML specification.
write_testtakers_xml <- function(testtakers, output = tempfile(fileext = ".xml")) {
  xml <- generate_testtakers(
    testtakers,
    custom_texts = custom_texts
  )
  xml2::write_xml(xml, output)
  output
}

output_18 <- write_testtakers_xml(testtakers)

# Wrapper pinned to the legacy Testcenter 16 schema location.
write_legacy_testtakers_xml <- function(testtakers, output = tempfile(fileext = ".xml")) {
  xml <- generate_testtakers(
    testtakers,
    custom_texts = custom_texts,
    app_version = "16.0.2",
    testtakers_version = "legacy-16"
  )
  xml2::write_xml(xml, output)
  output
}

output_legacy <- write_legacy_testtakers_xml(testtakers)
}
