% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_audio_video_plays.R
\name{estimate_audio_video_plays}
\alias{estimate_audio_video_plays}
\title{Estimate audio and video play counts from response JSONs}
\usage{
estimate_audio_video_plays(response_df)
}
\arguments{
\item{response_df}{Tibble. Response data retrieved with \link[=get_responses]{get_responses()} or
read with \link[=read_responses]{read_responses()}. Must contain \code{responses}, \code{group_id},
\code{login_name}, \code{login_code}, \code{booklet_id}, and \code{unit_key}. If \code{page_no} is
present, it is retained in the output.}
}
\value{
A tibble with one row per participant, unit, page, and media element.
It contains the session and unit columns, \code{media_id}, \code{media_type},
\code{status}, \code{n_plays}, and \code{media_key}. \code{media_key} is a namespaced compound
key of the form \code{unit_key__media__media_type__media_id} and is intended for
display or export, not as a replacement for the separate key columns.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Extracts audio and video response elements from the \code{responses} JSON column.
Media elements are identified by an \code{id} value that marks the element as audio
or video. Malformed or missing response JSONs are ignored.
}
